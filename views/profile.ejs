<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10 font-sans">
        
        <div class="flex justify-between items-center mb-10">
            <h3 class="text-3xl font-semibold">
                <span class="font-light text-zinc-300">Hello</span>, <%= user.name %> 👋🏻
            </h3>
            <a class="px-4 py-2 bg-red-500 hover:bg-red-600 transition rounded-md text-sm" href="/logout">Logout</a>
        </div>

        <div class="mb-10">
            <h4 class="text-lg text-zinc-300 mb-2">You can create a new post.</h4>
            <form action="/post" method="post">
                <textarea 
                    class="resize-none bg-zinc-800 border-2 border-zinc-700 rounded-md w-1/3 p-3 outline-none placeholder-zinc-500 focus:ring-2 focus:ring-blue-500" 
                    placeholder="What's on your mind?" 
                    name="content"></textarea>
                <input 
                    type="submit" 
                    class="px-4 py-2 bg-blue-500 hover:bg-blue-600 transition block rounded-md mt-2 w-40 text-sm font-medium" 
                    value="Create New Post">
            </form>
        </div>

        <hr class="border-zinc-700 mb-10">

        <div class="posts">
            <h3 class="text-xl font-medium text-zinc-400 mb-4">Your Posts</h3>
            <div class="postcontainer flex flex-wrap" style="gap: 8px;">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-[32%] p-4 border border-zinc-700 bg-zinc-800 rounded-xl mb-4 shadow-md hover:shadow-lg transition">
                        <h4 class="text-blue-400 mb-2 font-semibold">@<%= user.username %></h4>
                        <p class="tracking-tight text-sm text-zinc-200 leading-relaxed"><%= post.content %></p>
                        <small class="mt-3 inline-block text-xs text-zinc-400"><%= post.likes ? post.likes.length : 0 %> likes</small>
                        <div class="btns flex gap-4 mt-3 text-sm">
                            <a class="text-blue-400 hover:underline" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? 'Like' : 'Unlike' %>
                            </a>
                            <a class="text-zinc-400 hover:underline" href="/edit/<%= post._id %>">Edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>
