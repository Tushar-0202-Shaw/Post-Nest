<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10 font-sans">

        <div class="flex justify-between items-center mb-10">
            <h3 class="text-2xl font-semibold text-zinc-300">Edit Post</h3>
            <a class="px-4 py-2 bg-red-500 hover:bg-red-600 transition rounded-md text-sm" href="/logout">Logout</a>
        </div>

        <form action="/update/<%= post._id %>" method="post">
            <textarea 
                class="resize-none bg-zinc-800 border-2 border-zinc-700 rounded-md w-1/3 p-3 outline-none placeholder-zinc-500 focus:ring-2 focus:ring-yellow-500 text-sm leading-relaxed text-white" 
                placeholder="What's on your mind?" 
                name="content"><%= post.content %></textarea>
            <input 
                type="submit" 
                class="px-4 py-2 bg-yellow-500 hover:bg-yellow-400 text-black font-medium block rounded-md mt-3 w-40 text-sm transition" 
                value="Update Post">
        </form>

    </div>
</body>
</html>
